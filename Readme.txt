Project Overview:

Use the arrow keys to play Doodlejump! An executable JAR is included with "target", and project is configured as a Maven project. 

Note, this uses JavaFX and is only natively compatible with Java 8
